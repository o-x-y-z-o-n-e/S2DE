Refactor all static variables to use 's_' prefix.
Replace raw pointers with std::shared_ptr

S2DE Version System.
Physics collision system.
Texture Runtime Manipulation.
Texture Alias (Spritesheets).

Systems:
    Input
    Audio Playback
    Physics
    GUI
    Resource Manager (Textures, Audio)